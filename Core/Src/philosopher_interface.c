#include <stdint.h>
#include "philosopher_interface.h"

bool arr[5] = {0,0,0,0,0};
/*
void take_forks(uint8_t argument, uint8_t* state)
{
	state[argument] = HUNGRY;
	// osDelay(1);
	if (state[argument] == HUNGRY && state[LEFT] != EATING && state[RIGHT] != EATING)
	{
		// DO NOTHING
	}
}
*/
